// import { Socket } from 'socket.io';

// import { EVENT } from '../constants/socket.js';

// export const userEventController = (socket: Socket) => {
//   socket.on(EVENT.REQUEST_USER_ID, () => {
//     socket.emit(EVENT.RECEIVE_USER_ID, socket.id);
//   });
// };

export {};
